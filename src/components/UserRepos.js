import React, { Component } from 'react'

class UserRepos extends Component {
  render(){
    return (
      <h1> this is UserRepos. message: { this.props.username }</h1>
    )
  }
}

export default UserRepos
